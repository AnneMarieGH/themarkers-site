'use client'

// Legacy Sanity Portable Text renderer — kept for reference, not actively used.
// The site now uses MarkdownRenderer for article body content.

export function PortableTextRenderer({ value }: { value: unknown[] }) {
  void value
  return null
}
